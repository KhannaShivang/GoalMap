spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/goalplanner_test
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: create-drop
  ai:
    ollama:
      base-url: http://localhost:11434
      chat.options.model: llama3.2
      embedding.options.model: nomic-embed-text
    vectorstore:
      pgvector:
        dimensions: 768
        initialize-schema: true
